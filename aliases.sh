if [ -z "$SSH_CLIENT" ]
then
    alias -g vim='nvim'
fi

alias -s jpg=chromium
alias -s pdf=chromium
alias -s png=chromium

alias -s wmv=vlc

alias -s cfg=vim
alias -s md=vim
alias -s py=vim
alias -s rst=vim
alias -s txt=vim
